Majority of the tests are placed in "pods" style directory structure, meaning they grouped by feature and
placed in corresponding sub-directories.

This folder is used for configuration and some other high-level tests that are not feature specific.
